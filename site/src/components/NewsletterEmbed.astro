---
const {
  title = "weekly remote tips + fresh roles",
  sub = "no spam. unsubscribe anytime.",
  // your Buttondown embed URL
  src = "https://buttondown.com/remotelyyou?as_embed=true"
} = Astro.props;
---
<section class="card" style="max-width:560px">
  <h3 style="text-transform:lowercase;margin:0 0 6px 0">{title}</h3>
  <p class="muted" style="margin:0 0 12px 0">{sub}</p>
  <iframe
    src={src}
    scrolling="no"
    style="width:100%;height:220px;border:1px #ccc solid;border-radius:12px;background:#fff"
  ></iframe>
</section>
